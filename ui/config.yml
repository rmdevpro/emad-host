# Gradio Chat UI configuration
port: 7860

mads:
  - name: Context Broker
    url: http://emad-host-langgraph:8000
    hostname: emad-host-langgraph

# Health poll interval in seconds
health_poll_interval: 30
